!!!
%html
  %head
    %title hello
    - if @page_description
      %meta{:name => "description", :content => @page_description}
    = stylesheets :screen
    = javascripts('jquery-1.6.1.min')
    = javascripts('modernizr-1.7.min')
  %body{:id => body_id, :class => body_class}
    = yield

    = javascripts('application')
